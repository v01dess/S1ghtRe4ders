I suggest using a commandline when you're launching the server, go into the project's root folder and just use `./gradlew :server:run`

The entire thing is lan-based as i mentioned earlier, it is ass.
